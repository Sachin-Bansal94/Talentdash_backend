# ─── TalentDash Backend — Environment Variables ───────────────────────────────

# DATABASE URL (required)
# A live Neon database is already provisioned and seeded with 61 records.
# Copy this exact value into your .env file — no database setup needed.
#
# This project uses Neon PostgreSQL via serverless WebSocket driver (port 443)
# Works on all networks — no port 5432 required
#
DATABASE_URL="postgresql://neondb_owner:npg_lE1RjzMtdOW5@ep-solitary-hill-apssivjr.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require"

# PORT (optional — defaults to 3000)
PORT=3000